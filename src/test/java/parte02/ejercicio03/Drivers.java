package parte02.ejercicio03;

/**
 * Se puede usar esto para el catalogo de driver a usar, invocando en los hooks o fixtures
 *  o directamente en steps en caso de gherkin o en los unit test (junit, testng)
 */
public enum Drivers {
    WEB,
    ANDROID,
    IOS,
    DESKTOP,
    API
}
